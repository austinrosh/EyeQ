"""MSE-SNR & COM (Phase 2/5 — placeholder).

MSE-SNR at the decision point (the live "MSE SNR = ... dB" readout) and the
channel operating margin COM = 10*log10((A_signal/A_noise)^2) with
A_signal = A_peak/(M-1), A_noise = Q*sigma_n (Shakiba et al., Part I, Eq. 1).
"""
